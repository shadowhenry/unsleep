{
  "name": "unsleep",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "ios:build": "tauri ios build",
    "ios:open": "tauri ios build -- --open",
    "preview": "python3 -m http.server 5173 -d web"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
